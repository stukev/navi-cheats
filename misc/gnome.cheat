% gnome, process number

# Find process number of a task via mouse click
xprop | grep PID

# Open the current terminal path in file explorer
nautilus .

# Softly restart gnome (this simulates the same behaviour as alt + f2 -> r)
gdbus call --session --dest org.gnome.Shell --object-path /org/gnome/Shell --method org.gnome.Shell.Eval 'Meta.restart(_("Restarting…"))'

